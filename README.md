# CSC483-Final_Project
This aims to retrieve information from a csv that contains the metadata from songs


insert api key in IRSystem.py


for intializing this create python venv

pip install -r Requirements

pip install tkinter
